# 2026-05-23

## What I worked on

Set dev server port to 3000 in `vite.config.ts`.

## Decisions made

Added `server: { port: 3000 }` to Vite config so `npm run dev` consistently binds to localhost:3000.

## What I worked on

Updated `BASE_URL` in `scripts/snapshot.js` from port 5173 to 3000 to match the new dev server config.

## Next step

Run `npm run snapshot` against a live dev server to confirm screenshots still capture correctly.
